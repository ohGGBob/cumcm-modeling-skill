# 接题 5 分钟速查卡（最小闭环）

> 一切细节见 `SKILL.md`；本卡是"拿到题立刻照做"的压缩版。**能自动化就自动化，能一句判据抓住就绝不靠"看起来不对"。**

## 0′–1′　读题前先立规矩
- [ ] 建 `data_inventory.md`：每个附件 **文件/sheet/形状(行×列)/步长/物理含义/用途** 逐项实测（`openpyxl`），**清单上没有 = 没读**（§18，模板 `templates/`）。
- [ ] 公式**视觉核验**：分数/指数/上下标看原图，**不信纯文本提取**——`e^(−0.45/C)` 最易被压成 `e^(−0.45·C)`（§4.1）。

## 1′–2′　判据先行，别急着写代码
- [ ] 算 4 个数定模型/网格/步长：`α=k/(ρc_p)`、`Bi=hR/k`、渗透深度 `2√(Dt)`、`D/(h_mR)`（§17，模板 `criteria_generator_template.md`）。
- [ ] 写**期望量级自检表**：每个待求量先给物理范围，越界即查（§6.2）。
- [ ] 写**口径简报**并冻结：附录用哪组、表面边界、环境延拓、单位；**冻结后只许上报不许改**（模板 `caliber_brief_template.md`）。

## 2′–3′　算第一版
- [ ] 便宜自检先过：`场≡初值 → 场不动`、制造解 `lap(r²)=4`、环境=初值解仍初值（§6.1）。
- [ ] 记住：**守恒/收敛/解析解都是"求解器级"验证，抓不住公式读反**——必须另有"解读正确性 + 物理自洽"两层（§7）。

## 3′–4′　多路互证 + 对账
- [ ] 派 ≥2 路**真正独立**实现（不同人/不同离散），先画**谱系图**防同源假独立（§23）。
- [ ] 结果进 `agent_results_matrix.csv`，跑 `scripts/analyze.py matrix` 圈离群（§12）。
- [ ] 每个数字带来源标签 + 可靠度分级进 ledger；**一个输入无来源 ⇒ 整链无来源**（§19，模板 `provenance_ledger_template.md`）。

## 4′–5′　收口红线
- [ ] 匿名全文扫描（本地路径含用户名）、AI 声明官方句式、工具型号**不编造**、附录源程序逐个能跑（§15）。
- [ ] `结果 ↔ 代码 ↔ 论文` 三者一致；结论可追溯作废（§8/§10）。

## 兜底三问（任何时候答不上就停下）
1. 这个数字**能回溯到题面/附件/代码哪个键**？（§10）
2. 谁验过我？他是不是和我**同一套错误**？（§7/§20）
3. 这个倍数/百分比**有算式吗、我复算过吗**？（§22）