---
name: cumcm-modeling
description: 数学建模竞赛（CUMCM 高教社杯、美赛等）中如何用 AI 可靠地读题、建模、求解、审计与交付。当用户要求做数模题、复盘数模 AI 表现、或多代理协同求解+交叉验证时使用。核心是把本方法在 2026 国赛 A 题中的真实翻车与困难提炼成可复用的铁律、自检判据与工作流。
---

# 数模竞赛 AI 辅助求解与审计方法论

> 来源：对 `2026 CUMCM A 题《药材的烘干问题》`全工作区（21+ 条独立求解线、5 轮审理报告、6 条登记在案的失败模式）的系统复盘提炼。
> 目标：让 AI 在做数学建模题时**少犯错、错可被抓住、口径可追溯**。

---

## 0. 一句话总纲

**读题先读数据再读公式；每个数字带来源标签；题面没给的不许编造；能用一句判据抓住的错误，绝不靠"看起来不对"。**

---

## 1. 何时使用

- 用户把一道数模题（CUMCM/美赛/校赛）或题面 + 附件交给你，要求建模型、写代码、出结果、写论文。
- 用户要求复盘一次数模竞赛中 AI 的表现、整理失败经验。
- 用户要求多代理（子代理/parallel agents）协同求解同一题并做交叉验证与终审。

---

## 2. 标准工作流（按顺序，不可跳步）

1. **建数据清单（铁律四）**：先逐项读完题面 PDF + 全部附件（含输出模板 xlsx 的 sheet 名、列头、步长、小数位、合并单元格），写 `data_inventory.md`。**不读完不算懂，未登记项一律视为没读，禁止进入建模。**
2. **视觉核验公式**：题面公式的分数、指数、上下标必须用视觉读图（矢量对象/截图）核对，**不能信纯文本提取**（见 §4.1）。
3. **锁定统一口径文件**：动手前写一份口径简报（物性参数表、边界条件、判据、环境延拓、单位），所有求解方第 0 步先读它；发现口径问题只许"异常上报"，不许自行改口径继续跑。
4. **多路独立求解**：同一问至少 2 路**完全不同的数值路径**（不同离散/时间格式/工）独立实现；关键判定 3 路（纸面推导 / 数值复现 / 对抗复核）。
5. **先过便宜自检再跑全程**（见 §6），过了才允许写交付文件。
6. **交叉验证 + 离群审计**：把各路结果并排，找出离群值与根因（见 §7）。
7. **口径分歧显式声明**：无法数学裁定谁对的分歧（如动边界对流项含/不含），在论文假设节**显式声明**采用哪一派，绝不悄悄选。
8. **交付一致性核对**：论文声称 ↔ 代码实现 ↔ 交付物（xlsx/json/zip）三者逐项一致（见 §8）。

---

## 3. 铁律（优先级最高，与其它约定冲突时以此为准）

1. **不编造**：题目里没有的物理量 → 不许自己定义；题目里没有的数据 → 不许自己编造。卡住了**停下来上报**（给出【卡点】【位置】【缺什么】【为什么非它不可】【需要用户算什么】五要素），由用户手算，绝不拿"合理估计/记忆里的标准值"往下填。
2. **每个数字带来源标签**，统一五种：`[题面]` / `[附件·文件名]` / `[附录N]` / `[代码·文件·键]` / `[外部值·需声明]`；估算一律标 `[估算·禁用]`，**不得作为下游推导依据**。
3. **口径必须成套**：同一物理量多种口径（干基通量 vs 真实水质通量、膜口径 vs 体心口径、平台延拓 vs 末值冻结）引用时必须写明用哪种，同一段推导内不得混用。
4. **时刻必须对齐**：任何"比值/差值"必须写清分子分母各自的时刻与口径。
5. **未知量不当已知量**：表面值、通量、梯度都是解出来的，不用"我觉得大概这个量级"取一个数代入。
6. **数据完整性**：论文每个数字能回溯到 数据 → data_inventory → 代码 → results.json/xlsx → 论文 的完整链路。
7. **先落盘**：跑通基线就写 `resultN.xlsx`，再迭代加密网格；**宁可先交粗网格的正确解，不交没跑完的细网格**。

---

## 4. 高频翻车点（按严重度排序，均为真实事故）

### 4.1 公式/数据"读错"——最致命、传播最广

| 坑 | 正确 | 错误 | 后果（2026 A 题实证） |
|---|---|---|---|
| PDF 分数指数被文本提取丢掉分数线 | `D∝e^(−0.45/C)`（C 在分母） | `e^(−0.45·C)`（C 在分子） | 烘干时长 **57 h → 16.4 h**，Q1 表面水分 1.51 → 1.20；波及 4 条管线 |
| 温度单位 | 开尔文 K | 摄氏度 ℃ | `e^(−3850/T)` 量级全错 |
| 附件物理含义 | 附件1 的 0.02–0.05 是**空气湿度** | 误当**药材含水率**（初始 2.55） | 差两个数量级，直接翻车 |
| 同题多组参数串用 | 附录2/3/4 的 ρ/cp/k/D **各自不同、互不通用** | 代入错一组 | 结果系统性偏差 |

**对策**：分数结构用视觉读图（PDF 矢量分数线 / 截图放大）逐字核对；`exp(−A/C)` 与 `exp(−A·C)` 在写代码前后各 grep 一次；指数型扩散系数随 C 下降必须**衰减**，不衰减即读反。

### 4.2 边界条件与离散"装配"——数值求解最常见的自欺

1. **表面膜阻与半单元导热阻必须串联**：`1/U = 1/h + (Δr/2)/k`；并联会把等效 h 放大数百倍，把表面钉死在环境温度。
2. **表面半单元有独立的显式稳定限**：`Δt < (Δr/2)·ρc_p/h`，与体扩散限 `Δr²/(2D)` 是**两个都要满足的约束**，否则表面先 NaN。
3. **线性化不自洽 → 均匀场崩**：用守恒变量 `w=ρ(C)C` 时，储存项与通量项必须用**同一套 ρ**；否则均匀场不是解，一步把 C 从初值崩到 ~0。**最便宜判据：uniform-field test**——令场恒等于初值装配后解一次，解必须仍是初值（偏差 < 1e-9），否则停下别跑。
4. **极点单元 r=0 面心≠体心 → 一阶精度退化**（实测阶≈−1.00），第 4 位小数不可信：用 `4α(T₁−T₀)/Δr²` 的 L'Hôpital 形式处理。
5. **表面列未解析边界层**：网格粗时最外单元会落进边界层、单元平均可能变负；表面列取值需声明**口径**（膜口径反解真表面值 vs 最外体心值，可差 0.016 ℃，涉手写表 14 个数字）。

### 4.3 物理建模的"口径分歧"——不是算错，但必须显式声明

- **题面未给的常数**（如汽化潜热 Lv）：不得自行取标准值继续算；必须上报，由用户决定取值 + 灵敏度方案。强塞会导致系统性偏冷甚至表面负温。
- **干基 vs 真实水质通量**：差一个 ρ_dry（≈ 数百到上千倍），漏乘会把"潜热是主导项"误判成"小项可忽略"。
- **动边界对流项（挤水项）含/不含**：同一题可差 ~2 h（≈4%），是全行业的真实分歧；论文必须选一派并声明，注明与另一派的差值与原因。
- **环境/边界的长时延拓**（如数据只给 0–4 h，要模拟 50+ h）：平台均值 vs 末值冻结差 ~0.5%，二选一并在假设节声明。

### 4.4 同一套交付物"自相矛盾"——最隐蔽的错误

- 症状：Q3 干燥达标而 Q4 几乎不动（100 h 才降 4%）。
- 根因（2026 A 题实证）：Q2/Q3 用 `D·∂C/∂r`，Q4 却换成 `ρD·∂C/∂r` 且控制体容量用 `(ρC)'≈1189`，量级差 ~1000 倍，表面被锁死。
- **判据**：同一物理系统、不同模型变体之间，量级必须自洽；出现"一个达标一个不动"先怀疑**口径不一致的 bug**，而不是"物理本来就慢"。

---

## 5. 三组附录公式/参数的防错口诀

1. 扩散系数指数一律 `exp(−常数/量)`（量在分母）；温度一律 K。
2. 变物性 `ρ(c_p)·D` 动态范围可达数个数量级，**一次全隐式 ≠ 正确**，必须用退化判据卡住。
3. 结果表**保留位数**按题面模板（常见 4 位小数），工作表名、A 列时间步长、首行距离列头逐格核对模板。

---

## 6. 数值求解自检清单（便宜、与实现无关，跑全程前先过）

| 判据 | 做法 | 通过标准 |
|---|---|---|
| S1 环境≡初值 | 令边界环境恒等于初值，跑一步 | 场不动（max\|Δ\| < 1e-9） |
| UT1 追赶/线性求解器 | 三对角系统已知解回代 | 误差 < 1e-14 |
| UT2 均匀场 | 装配后对常数场解一次 | 仍为常数（< 1e-9） |
| UT3 常数场算子 | 离散算子作用于常数 | = 0（< 1e-12） |
| S2 制造解 | 拉普拉斯作用于 `T=r²`（圆柱） | 内部=4（符号随约定，量值对） |
| 守恒 | 逐步相对残差 | < 1e-9 |
| 护栏 | C < 0 即终止（否则 `exp(−A/C)` 变 `exp(+A·|C|)` 爆炸）；T 越界即终止 | 每步登记 min(C)、range(T) |

> **S1/UT2 是最廉价的"装配正确性"判据，1 秒内抓出"线性化不自洽"这类结构错误。**

---

## 7. 多路互证方法论（防"多套实现共享同一错误口径"）

- **三层防线，缺一层视为未完成**：
  1. 与实现无关的解析判据（级数解/erfc 解/守恒律/制造解）；
  2. 外部约束（题面自述如"烘干 2–3 天"、量级上限如感热 ≤ h·ΔT）；
  3. 独立路径互证（不同离散、不同时间格式、不同工具）。
- 只做第 3 层不够——多套实现可能共享同一个错误口径而一致"通过"。
- **离群分析**：把各路关键数值并排放置，离群值逐个定位根因（读公式读反 / 边界多了一项 / 口径不同），标注"弃用 / 需修复 / 可解释口径差"三档。
- **共识区间**：取正确族的区间而非单一数值（如 Q3 = 57.1–57.5 h），并额外做**外部公开数值对照**（网上复盘、抖音/CSDN 可见值）作独立印证。

---

## 8. 交付与论文纪律

1. **论文声称 ↔ 代码 ↔ 交付物三者一致**：声明"不计入潜热"但代码里算了、支撑材料 zip 缺实际生成结果的那个脚本 → 均属不可复现的硬伤。
2. **引用数字必须可复现**：写"四路互差 ≤ 0.032 ℃"却复现不出（实测 0.0022）→ 证据链断裂，比不引用更危险；写实测值并注明口径。
3. **不留占位符/残稿**：摘要与正文不得残留"待补/占位符/某问未完成"；过时文档（作废结论、旧 HANDOVER）必须标 `[OBSOLETE]` 或归档，禁止被下游继续引用。
4. **过时结论必须物理清除**：同一文档里自相矛盾的两段（如 0.272 vs 2674 W/m²）会让整篇论文失信。
5. **AI 工具声明如实填写**：2026 国赛要求工具名称/版本/用途逐环节填报，**虚假声明取消资格**；名称/型号是占位符就必须由参赛者填，不代填不编造。
6. **时间与流程红线**：在线评测模拟器有硬截止（提前于正式截稿数小时）；正式测试机会有限（如每问 3 次，中止也算一次）；加密日志/文件名不得修改。

---

## 9. 多代理协同的工程纪律（父会话与子代理分工）

- **父会话不下场写码/改码/试错**：只做"定义任务（写可证伪命题 + 可计算判据）→ 派发调度（文件前缀隔离）→ 归纳裁决 → 汇报待拍板项"。亲自下场会陷入长时间单线程试错、挤占赛程，并失去"多路独立"这个最重要的纠错机制。
- **遇异常先派子代理，不自己分析**：把现象写成可证伪命题 + 量化判据，派 2–3 路不重叠子代理，归纳冲突，冲突未解再派一轮。
- **口径穿透**：统一口径文件列为人人第 0 步必读；改动已有产物必须派"改动人 + 非同一人的复核人"两路。

---

## 10. 一份可直接照抄的"接题 Checklist"

- [ ] 题面 + 附件 + 输出模板逐项读完，登记 `data_inventory.md`
- [ ] 公式分数/指数/单位已视觉核验（标出所有 `exp(−A/C)` 与 K）
- [ ] 明确每问用哪组参数（附录间不串用）
- [ ] 统一口径文件已定（物性、边界、判据、延拓、单位）
- [ ] 边界：膜阻串联；表面半单元稳定限已算
- [ ] S1 / UT2 / 制造解 自检已过（再跑全程）
- [ ] 多路独立互证 + 离群根因已定位
- [ ] 分歧口径已在论文假设节显式声明
- [ ] 数字全部可回溯（来源标签 → results → 论文）
- [ ] 交付物与论文逐格一致，无占位符、无过时结论
- [ ] AI 使用声明由参赛者如实填写

---

## 附录：本方法论的实证出处（2026 CUMCM A 题）

- 失败模式清单：`cumcm2026A/report/FAILURE_MODES_verified.md`（5 个已验证失败案例 + UT1–UT4/S1/S2 判据）
- 研究纪律：`cumcm2026A/report/DISCIPLINE_no_fabrication.md`（六条铁律 + 6 条历史事故）
- 项目纪律：`cumcm2026A/report/PROJECT_DISCIPLINE.md`、`cumcm2026A/report/FAILURE_MODES_verified.md`
- 离群与共识：`AGENT_RESULTS_SUMMARY_20260913.md`、`agent_results_matrix.csv`、`A题成果全面审理汇总_20260912.md`
- 完整复盘见本仓库 `docs/lessons-learned.md` 与 `docs/workspace-inventory.md`。