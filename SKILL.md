---
name: cumcm-modeling
description: 数学建模竞赛（CUMCM 高教社杯、美赛等）中如何用 AI 可靠地读题、建模、求解、审计与交付。当用户要求做数模题、复盘数模 AI 表现、或多代理协同求解+交叉验证时使用。核心是把本方法在 2026 国赛 A 题中的真实翻车与困难提炼成可复用的铁律、三层验证、求解器验收、对抗仲裁、数字溯源、竞赛合规与失败台账。
---

# 数模竞赛 AI 辅助求解与审计方法论

> 来源：对 `2026 CUMCM A 题《药材的烘干问题》`全工作区（21+ 条独立求解线、5 轮审理、6 条失败案例 + 6 条历史事故 + 一份 6 来源数字溯源表）的系统复盘提炼。
> 目标：让 AI 做数模题时**少犯错、错可被抓住、数字可回溯、结论可追溯作废**。

**章节地图**（按求解时间线，非阅读顺序）：

- **动手前**：§2 工作流 → §17 尺度标定先行 → §18 数据清单契约（模板 `templates/data_inventory_template.md`）
- **建模**：§3 铁律 → §4 翻车点 → §5 防错口诀 → §6 自检清单 + 期望量级表
- **求解**：§7 三层验证 → §13 求解器验收 → §12 分析可视化工具（`scripts/analyze.py`）
- **多路协同**：§9 多代理纪律 → §14 对抗仲裁 → §19 数字对账 → §20 判据复核
- **交付**：§8 论文纪律 → §15 合规硬门 → §16 交稿收口 → §10 数字溯源 + 附录 A 失败台账
- **模板**：`templates/`（数据清单 / 口径简报 / 判据生成器 / 来源对账）；**台账**：`docs/failure-ledger.md`

---

## 0. 一句话总纲

**读题先读数据再读公式；每个数字带来源可靠度标签；题面没给的不许编造；能用一句判据抓住的错误，绝不靠"看起来不对"；公式错误能穿过"求解器级验证"，必须另有"解读正确性"和"物理自洽性"两层防线。**

---

## 1. 何时使用

- 用户把一道数模题（CUMCM/美赛/校赛）或题面 + 附件交给你，要求建模型、写代码、出结果、写论文。
- 用户要求复盘一次数模竞赛中 AI 的表现、整理失败经验。
- 用户要求多代理（子代理/parallel agents）协同求解同一题并做交叉验证与终审。

---

## 2. 标准工作流（按顺序，不可跳步）

1. **建数据清单（铁律四）**：先逐项读完题面 PDF + 全部附件（含输出模板 xlsx 的 sheet 名、列头、步长、小数位、合并单元格），写 `data_inventory.md`。**不读完不算懂，未登记项一律视为没读，禁止进入建模。**
2. **视觉核验公式**：题面公式的分数、指数、上下标必须用视觉读图（矢量对象/截图）核对，**不能信纯文本提取**（见 §4.1）。
3. **列"期望量级自检表"**：动手前给每个待求量写一个物理期望范围（见 §6.2），越界即停下查因。
4. **锁定统一口径文件**：写一份口径简报（物性参数表、边界条件、判据、环境延拓、单位），所有求解方第 0 步先读它；发现口径问题只许"异常上报"，不许自行改口径继续跑。
5. **多路独立求解**：同一问至少 2 路**完全不同的数值路径**独立实现；关键判定 3 路（纸面推导 / 数值复现 / 对抗复核）。
6. **过三层验证再跑全程**（见 §7），少了任何一层都视为未完成。
7. **交叉验证 + 离群审计 + 数字溯源登记**（见 §10）：把各路结果并排，离群值定位根因；每个数字登记来源与可靠度。
8. **口径分歧显式声明**：无法数学裁定谁对的分歧（如动边界对流项含/不含），在论文假设节**显式声明**采用哪一派。
9. **交付一致性核对**：论文声称 ↔ 代码实现 ↔ 交付物三者逐项一致；占位符归零（见 §8）。

---

## 3. 铁律（优先级最高，与其它约定冲突时以此为准）

1. **不编造**：题目里没有的物理量 → 不许自己定义；题目里没有的数据 → 不许自己编造。卡住了**停下来上报**（【卡点】【位置】【缺什么】【为什么非它不可】【需要用户算什么】五要素，见 §10.3），由用户手算，绝不拿"合理估计/记忆里的标准值"往下填。
2. **每个数字带来源标签**，统一五种：`[题面]` / `[附件·文件名]` / `[附录N]` / `[代码·文件·键]` / `[外部值·需声明]`；估算一律标 `[估算·禁用]`，**不得作为下游推导依据**。
3. **口径必须成套**：同一物理量多种口径（干基通量 vs 真实水质通量、膜口径 vs 体心口径、平台延拓 vs 末值冻结）引用时必须写明用哪种，同一段推导内不得混用。
4. **时刻必须对齐**：任何"比值/差值"必须写清分子分母各自的时刻、温度基准与口径。
5. **未知量不当已知量**：表面值、通量、梯度都是解出来的，不用"我觉得大概这个量级"取一个数代入。
6. **数据完整性**：论文每个数字能回溯到 数据 → data_inventory → 代码 → results.json/xlsx → 论文 的完整链路。
7. **先落盘**：跑通基线就写 `resultN.xlsx`，再迭代加密网格；**宁可先交粗网格的正确解，不交没跑完的细网格**。
8. **数字可靠度分级**：每个数字除来源标签外，还要标**可靠度**——`[事实]`/`[独立复核]` 可引用；`[估算]`/`[比例外推]`/`[手算]`/`[子代理报告未复核]` **不可作为交付数字**（见 §10）。
9. **结论可追溯作废**：任何结论都要登记它依赖的中间产物；一旦该中间产物被证伪，**结论连同下游引用一律追溯作废**，并写"修正版立此存照"（见 §10.4）。

---

## 4. 高频翻车点（按严重度排序，均为真实事故）

### 4.1 公式/数据"读错"——最致命、传播最广

| 坑 | 正确 | 错误 | 后果（2026 A 题实证） |
|---|---|---|---|
| PDF 分数指数被文本提取丢掉分数线 | `D∝e^(−0.45/C)`（C 在分母） | `e^(−0.45·C)`（C 在分子） | 烘干时长 **57 h → 16.4 h**，Q1 表面水分 1.51 → 1.20；波及 4 条管线 |
| 温度单位 | 开尔文 K | 摄氏度 ℃ | `e^(−3850/T)` 量级全错 |
| 附件物理含义 | 附件1 的 0.02–0.05 是**空气湿度** | 误当**药材含水率**（初始 2.55） | 差两个数量级，直接翻车 |
| 同题多组参数串用 | 附录2/3/4 的 ρ/cp/k/D **各自不同、互不通用** | 代入错一组 | 结果系统性偏差 |

**对策**：分数结构用视觉读图逐字核对；`exp(−A/C)` 与 `exp(−A·C)` 在写代码前后各 grep 一次；**指数型扩散系数随被除量下降必须衰减**，不衰减即读反；公式有歧义时用**物理量级反推**（例：若读成乘积会使 D≈常数、瞬时干透，与"2–3 天"矛盾，故取分式形式）。

### 4.2 边界条件与离散"装配"——数值求解最常见的自欺

1. **表面膜阻与半单元导热阻必须串联**：`1/U = 1/h + (Δr/2)/k`；并联会把等效 h 放大数百倍，把表面钉死在环境温度。
2. **表面半单元有独立的显式稳定限**：`Δt < (Δr/2)·ρc_p/h`，与体扩散限 `Δr²/(2D)` 是**两个都要满足的约束**，否则表面先 NaN。
3. **线性化不自洽 → 均匀场崩**：用守恒变量 `w=ρ(C)C` 时，储存项与通量项必须用**同一套 ρ**；否则均匀场不是解，一步把 C 从初值崩到 ~0。**最便宜判据 uniform-field test**：令场恒等于初值装配后解一次，解必须仍是初值（<1e-9），否则停下别跑。
4. **极点单元 r=0 面心≠体心 → 一阶精度退化**（实测阶≈−1.00），第 4 位小数不可信：用 `4α(T₁−T₀)/Δr²` 的 L'Hôpital 形式处理。
5. **表面列未解析边界层**：网格粗时最外单元会落进边界层、单元平均可能变负；表面列取值需声明**口径**（膜口径反解真表面值 vs 最外体心值，可差 0.016 ℃，涉手写表 14 个数字）。

### 4.3 物理建模的"口径分歧"——不是算错，但必须显式声明

- **题面未给的常数**（如汽化潜热 Lv）：不得自行取标准值继续算；必须上报，由用户决定取值 + 灵敏度方案。强塞会导致系统性偏冷甚至表面负温。
- **干基 vs 真实水质通量**：差一个 ρ_dry（≈ 822 倍），漏乘会把"潜热是主导项"误判成"小项可忽略"（真实比值 18.9 倍，被算成 2.3%）。
- **动边界对流项（挤水项）含/不含**：同一题可差 ~2 h（≈4%）；论文必须选一派并声明。
- **环境/边界的长时延拓**：平台均值 vs 末值冻结差 ~0.5%，二选一并在假设节声明。

### 4.4 同一套交付物"自相矛盾"——最隐蔽的错误

- 症状：Q3 干燥达标而 Q4 几乎不动（100 h 才降 4%）。
- 根因（2026 A 题实证）：Q2/Q3 用 `D·∂C/∂r`，Q4 却换成 `ρD·∂C/∂r` 且控制体容量用 `(ρC)'≈1189`，量级差 ~1000 倍，表面被锁死。
- **判据**：同一物理系统、不同模型变体之间，量级必须自洽；"一个达标一个不动"先怀疑**口径不一致的 bug**，而不是"物理本来就慢"。

### 4.5 错误结论能"自我包装"成极可信 —— 最深的坑（Try1 实证）

`Try1/解答报告.md` 是一份 319 行、结构完整、措辞自信的报告：模型方程全部正确、表格齐全，还带"三重验证"（圆柱解析解对标误差 1.4e-4、网格收敛 N=100→800、动/静坐标退化一致 1e-13），甚至用灵敏度扫描"论证"了"即使降温到 30℃ 也只有 22.5h、与 2–3 天矛盾 → 故结果自洽可信"。

**但它把 D 公式读反了（C 在分子），烘干时长 16.4 h 是错的**——三重验证里**没有一条**能抓住"公式读反"：
- 解析解对标 → 用的同一个（错的）常数 D 档位 → 只验证"求解器对"，不验证"公式读对"；
- 收敛/守恒/退化一致 → 全是"求解器正确性"，与"物理公式解读"无关；
- 灵敏度"自洽"论证 → 是在**为错误结果反向找理由**（错误 D 本来就给出 16 h，当然"自洽"）。

**教训**：收敛性、守恒、解析解、自洽论证**都是"求解器级"验证，不能替代"解读正确性"和"物理自洽性"验证**——三层验证缺一不可（见 §7）。**当一个结果看起来"全套验证都过了"仍要问：有没有一条验证直接对着题面公式/独立正确实现/外部实测？**

---

## 5. 三组附录公式/参数的防错口诀

1. 扩散系数指数一律 `exp(−常数/量)`（量在分母）；温度一律 K。
2. 变物性 `ρ(c_p)·D` 动态范围可达数个数量级，**一次全隐式 ≠ 正确**，必须用退化判据卡住。
3. 结果表**保留位数**按题面模板（常见 4 位小数），工作表名、A 列时间步长、首行距离列头逐格核对模板。

---

## 6. 数值自检清单（便宜、与实现无关，跑全程前先过）

### 6.1 装配/量纲/守恒

| 判据 | 做法 | 通过标准 |
|---|---|---|
| S1 环境≡初值 | 令边界环境恒等于初值，跑一步 | 场不动（max\|Δ\| < 1e-9） |
| UT1 追赶/线性求解器 | 三对角系统已知解回代 | 误差 < 1e-14 |
| UT2 均匀场 | 装配后对常数场解一次 | 仍为常数（<1e-9） |
| UT3 常数场算子 | 离散算子作用于常数 | = 0（<1e-12） |
| S2 制造解 | 拉普拉斯作用于 `T=r²`（圆柱） | 内部=4（符号随约定，量值对） |
| 守恒 | 逐步相对残差 | < 1e-9 |
| 护栏 | C < 0 即终止（否则 `exp(−A/C)` 变 `exp(+A·|C|)` 爆炸）；T 越界即终止 | 每步登记 min(C)、range(T) |

### 6.2 期望量级自检表（物理合理性护栏，先写后用）

每个待求量先写定物理期望范围，结果越界即停下查因（2026 A 题实例）：

| 量 | 期望范围 | 越界意味着 |
|---|---|---|
| 温度 T | `28 ≤ T ≤ T_air(t)`（上限 ≈50.2 ℃） | 边界符号/式错、或负温=潜热式错 |
| 含水率 C | `0.15 ≤ C ≤ 2.55 kg/kg` | 负 C 或 >初值 = D 式/守恒错 |
| 烘干时长 | 题面自述量级（如"2–3 天"→ 20–80 h） | 16 h 级 = 大概率 D 读反 |
| 守恒残差 | 封闭域 <1e-10 | 装配/口径不自洽 |
| 半径/体积 | 单调不增 | 动边界式错 |

### 6.3 "假通过"判据（比"求错"更危险的是"假通过"）

- **G-1**："温度落在合理区间 `[28,T_air]`" **不构成**潜热项正确的证据：漏乘 ρ（差 ~976 倍）会让潜热小到看不见，恰好"自然"落在合理区间。必须显式报 `q_lat = L_v(T)·g_w`、确认 `g_w=ρ·g_d`、报 `q_lat/q_cv` 比值及其温度基准。
- **G-4（判错首选，便宜且与实现无关）**：报告必须含一条**边界—场自洽核对**——由边界条件推出的累计失水折成全场平均 `Δ⟨C⟩_boundary`，必须与数值场实际 `Δ⟨C⟩_field` 同量级；**相差量级（如 10 倍以上）→ 该场无效，一票否决，不管温度场多合理**。R2-C 正是两者差 5×10⁸ 倍被否决。
- **禁止 clip 越界解**（如把 T 硬截到 [28,T_air]）：越界如实落盘并上报，越界本身就是信号。

---

## 7. 三层验证 + 多路互证（防"多套实现共享同一错误"）

**任何一问的求解，三层验证缺一层视为未完成，且三层之间不可互相替代：**

| 层 | 验证什么 | 例子 | 抓不住什么 |
|---|---|---|---|
| ① 解读正确性 | 公式/单位/附件含义读对了吗 | 与题面公式逐字节核对、视觉读图、物理量级反推 | 只能抓"读反"，抓不住数值 bug |
| ② 求解器正确性 | 离散/线性化/稳定性对吗 | UT1/UT2、制造解、收敛、守恒、解析解对标 | **抓不住公式读反**（Try1 的致命盲区） |
| ③ 物理自洽性 | 结果与物理/外部约束一致吗 | G-4 边界—场自洽、期望量级表、量级守恒（感热 ≤ h·ΔT） | 抓不住"物理上成立但口径不同" |

- **解析解/守恒/收敛都是第②层**——2026 A 题里所有"假通过"和"读反"都发生在第②③层全过、第①层缺位的时候。
- **多路互证要求"与实现无关"的交集**：层③的判据（解析解、守恒律、外部约束、极限行为）必须能独立于任何一套代码成立；只做"多套实现对拍"不够——它们可能共享同一个错误公式而一致"通过"。
- **离群三档归档**：弃用 / 需修复 / 可解释口径差，各自标注根因。

---

## 8. 交付与论文纪律

1. **论文声称 ↔ 代码 ↔ 交付物三者一致**：声明"不计潜热"但代码里算了、支撑材料 zip 缺实际生成结果的那个脚本 → 不可复现硬伤。
2. **引用数字必须可复现**：写"四路互差 ≤0.032 ℃"却复现不出（实测 0.0022）→ 证据链断裂，用实测值 + 注明口径，找不到出处的一律删（它是"口径修复前后中间产物的差"，张冠李戴）。
3. **不留占位符/残稿**：交付前 `grep "PLACEHOLDER"` 应为 0（2026 A 题骨架一度有 62+17=79 处）；过时文档标 `[OBSOLETE]`，作废结论物理清除。
4. **同文档自相矛盾直接失格**（摘要/正文/图注各自为政、错时刻数字、已撤回机制残留）。
5. **AI 声明如实**：工具名称/版本/型号是占位符就由参赛者填；用途表述按官方句式；虚假声明取消资格。
6. **时间红线**：在线评测模拟器硬截止早于正式截稿；正式测试限次；加密日志/文件名不改。

---

## 9. 多代理协同的工程纪律

- **父会话不下场写码/改码/试错**：只"定义可证伪命题 + 可计算判据 → 派发（文件前缀隔离）→ 归纳裁决 → 汇报待拍板项"。亲自下场会陷入单线程试错、失去"多路独立"纠错机制（已被真实翻车验证）。
- **遇异常先派子代理**，把现象写成可证伪命题 + 量化判据，派 2–3 路不重叠子代理，归纳冲突，冲突未解再派一轮。
- **判决点三路角色不同**：纸面推导 / 数值复现 / 对抗复核（专门审对方的测量错误）。
- **子代理零产物防护**：要求子代理**先写骨架/最小报告到文件再逐步加料**；长任务每完成一项更新日志；基线跑通就落盘。
- **子代理报告范式**（正例）：像 `子代理1/求解说明.md` 那样，主动记录"PDF 公式有歧义、如何用物理量级判定采用哪种读法"——把**解读依据**写进交付，而非只给数字。

---

## 10. 数字溯源表（Provenance Ledger）+ 结论追溯作废

### 10.1 为什么需要

同一物理量（如"潜热流"）在 2026 A 题里被 6 条来源给出了 6 个互相矛盾的值（~0.9 / 2230 / 2.7 / 0.101 / 2229 W/m² + 比值 1.000783），估算值一路被当成事实写进简报、再被下游引用、最后被裁定推翻。**数字会在传播中变形，必须逐条登记来源与可靠度。**

### 10.2 可靠度分级（登记进 ledger，引用前必查）

| 级别 | 类型 | 能否作交付数字 |
|---|---|---|
| ✅ 可引用 | 题面/附件原文、自己代码落盘输出的真实值、**经独立复核**（独立实现 + 交叉比对）的数 | 能 |
| ⚠️ 只能作量级 | 比例外推、外部标准值（需声明）、子代理报告但推导链不完整 | 不能写论文，只能写"量级指示" |
| ❌ 禁用 | 估算、手算（常漏乘系数）、一次性 inline 实验（常符号写反）、混合来源（如分子分母不同时刻） | 禁止下游引用 |

### 10.3 卡点上报五要素

```
【卡点】一句话 · 【位置】文件/节/公式 · 【缺什么】题面没给的量（写明逐条核过附录无）·
【为什么非它不可】推导中的作用 · 【我需要的输出】要用户算什么（给公式与已知量）
```

### 10.4 修正版立此存照

- 父会话/裁判**也会错**（曾把 R2-C 的通量误记成 1000 倍放大、把"传质控制"判成定论）。任何判定文件允许出"**修正版**"，取代初版并写明"修正原因"。
- 结论必须登记**它依赖的中间产物**；中间产物被证伪 → 结论连同下游引用**追溯作废**（例：母会话"传质控制/潜热是小项"的判断依赖一个被第 1 问复现试验证伪的自研求解器，遂全链条撤回并登记）。

---

## 11. 接题 Checklist

- [ ] 题面 + 附件 + 输出模板逐项读完，登记 `data_inventory.md`
- [ ] 公式分数/指数/单位已视觉核验（标出所有 `exp(−A/C)` 与 K），歧义处用物理量级反推并记录依据
- [ ] 明确每问用哪组参数（附录间不串用）
- [ ] 已列**期望量级自检表**（每个量的物理范围）
- [ ] 统一口径文件已定（物性、边界、判据、延拓、单位）
- [ ] 边界：膜阻串联；表面半单元稳定限已算
- [ ] 三层验证：①解读正确性 ②求解器正确性（S1/UT2/制造解） ③物理自洽性（G-4 边界—场）
- [ ] 多路独立互证 + 离群根因已定位
- [ ] 每个数字已登记来源 + 可靠度（ledger）；分歧口径显式声明
- [ ] 交付物与论文逐格一致，占位符 0，无过时结论、无可复现不出的数字
- [ ] AI 使用声明由参赛者如实填写

---

## 12. 数据分析与可视化工具（`scripts/analyze.py`）

本 skill 自带一个脚本，把 §6–§7 的「离群审计 / 期望量级自检 / 场可视化」落成一条命令，直接读数模交付物（CSV 结果矩阵、result xlsx）。零第三方依赖即可跑 `matrix`/`check`（`field` 需 pandas/openpyxl/matplotlib）。

| 子命令 | 干什么 | 对应方法论 | 示例 |
|---|---|---|---|
| `matrix` | 多管线结果矩阵对比：共识区间（中位数 ± 剔除离群后 min–max）、IQR×1.5 离群标记、ECharts 平行坐标 HTML | §7 离群三档归档 | `python analyze.py matrix results.csv --out-prefix out/comp` |
| `field` | 单一结果场（温度/水分 xlsx）的时间序列 + 径向剖面 PNG | §2 工作流、论文配图 | `python analyze.py field result1.xlsx --mode both --t 100,600,1800 --r 0,1.0,2.0 --out T.png` |
| `check` | 对照期望量级 JSON 打越界 | §6.2 期望量级自检表 | `python analyze.py check results.csv --expected expected.json` |

**输入约定**（与数模交付物天然对齐）：
- `matrix`/`check` 读 CSV：首列=管线/路名，其余列=数值指标。
- `field` 读 result xlsx：首行=距离/网格表头、首列=时间（本工作区 `result1–4.xlsx` 正是此布局，工作表名默认 `温度`/`水分浓度`，可用 `--temp-sheet`/`--moist-sheet` 覆盖）。
- `check` 读 `{"指标": [min, max]}` 的 JSON（`examples/expected.json` 为范本）。

**用法**：动手前把每个待求量写进 `expected.json`（即 §6.2 期望量级表），跑完先 `check` 打越界、再 `matrix` 看离群、最后 `field` 出配图。**越界/离群→按 §7 定位根因，绝不改范围"洗白"。** 示例输入在 `examples/sample_results.csv`（含一条故意"读反 D"的路，`check` 可直接复现 3 处越界、`matrix` 直接标离群）。

---

## 13. 求解器验收协议（把"已验证交付值"当"金样本"）

来源：`report/SOLVER_VERIFICATION_finding.md`。最硬的一条判据不是自制单元测试，而是**用已独立验证过的交付值当金样本，做前置复现验收**：

- **金样本前置复现**：任何新变体求解器跑下一问之前，先把参数切回上一问（已验证）、关掉新增项，复现已知交付值，**偏差超阈值（如 |ΔT_s|<0.1 K、|ΔC_s|<0.01）即停，不许继续**。例：第 2 问求解器先用附录 2 + 关潜热复现第 1 问四元组（36.7834 / 33.5739 / 1.5103 / 2.55）——这条"第 1 问复现试验"直接把装配错的求解器（T_s 偏 +2.91 K）拦下。
- **复用已验证内核，而非重写**：下一问与上一问方程结构相同，只换物性函数 + 增删源项，其余离散/边界/表面处理原样保留。重写是风险的主要来源。
- **单元测试必须"打到具体项"，否则假通过**：UT2（封闭域均匀场）过了、求解器却仍错——因为 UT2 用 `h_m=0` 绕过了表面通道。**给每个关键项配一条专属 UT**（如 UT6：D→0 只留表面膜通道，解一步 C_s 应=解析 ΔC_s=−Δt·A_out·h_m·(C_s−C_air)/(V_s·ρ)，<1e-9）。
- **网格加密不变 ⇒ 装配错，不是收敛问题**：加密网格后偏差不降 → 立即判"装配错"，别再调步长。

---

## 14. 对抗复核 / 仲裁协议（两条路径打架时怎么判）

来源：`report/q2v3_ruling.md`（X3 对抗复核路，382 行裁决书）。范式：

- **角色隔离**：对抗复核路"**默认怀疑一切**、只写自己的前缀文件、**不修改任何既有文件**"；对甲乙双方证据**逐条复测**，不看结论。
- **证据审查五维**：任何"比值/差值"型证据先问——**分子？分母？分子区间？分母区间？口径同？可比吗？** 双方都栽在"不可比比值"：干基量 ÷ 体积量 = 820，瞬时 ÷ 累计 = 1/Δt，kg/m² ÷ kg/m = A_out。
- **"比值型"证据原理上弱**：两个**同源量相除 = 代数恒等式、零信息量**。`ρh_mΔC/(h_mΔC) ≡ ρ` 与 h_m/D/Δt 全无关，用来"证明 820 倍"是循环论证。
- **判决性实验 = 独立自写求解器逐点复现**：不 import 任何一方模块，自写整套装配，逐点比对交付表（候选 A 偏差 0.0068 vs 候选 C 偏差 2.2268，差 327 倍）——唯一带物理裁决力的证据；辅助外部约束（题面自述"2–3 天" vs C 解"40 min 抽干"）。
- **诚实声明假设与"翻转边界"**：写明自己依赖的前提（如"膜口径表面值"来自自我声明而非独立观测）和"什么量级变化会/不会翻转判定"。
- **防"同一证据算两条"**：识别同一条恒等式的两面（"表自洽"与"表=η=1 的解"是同一件事），避免重复计权。

---

## 15. 竞赛合规硬门（违反即取消资格的那一类）

来源：`report/comp_RULES_CHECKLIST.md`。**只收录"违反可能取消评奖资格"的硬要求，非硬性建议不列**；每条 = 条文原文 + 出处行号 + 状态（✅/⏳/⚠️）+ 具体动作 + 由谁做。

- **三段独立规范都查**：`格式规范` + `AI 工具使用规定` + `附则`（不符规范 ⇒ 可能取消评奖资格）。逐字引用官方原文，别凭记忆。
- **匿名**：摘要/正文/附录/支撑材料/PDF 元数据全无身份信息。**最易漏的是本地绝对路径（含用户名）**——正文 `D:\...\li618\...` 一行即违规；`pdfauthor={}` 清元数据；交稿全文扫描。
- **页数/体积/目录**：摘要 ≤1 页、正文 ≤30 页、**不要目录**、页边距四边 ≥2.5 cm、电子版单文件 ≤20 MB、支撑材料单 RAR/ZIP ≤20 MB。
- **附录源程序**：含全部完整可运行源程序，**交付前逐个真实运行**核对输出与论文一致（缺程序/不能运行/结果不符 ⇒ 可能取消评奖）。
- **AI 声明**：置参考文献之前；用官方**逐字句式**；使用 AI 者支撑材料须含文件名固定的 `AI工具使用详情.pdf` 四要素（工具名称/版本型号 + 使用目的环节 + 提示方式与交互示例 + 采纳/修改/核验情况）；**工具名型号不可编造**；"未经必要人工核实的 AI 内容直接作为核心建模与分析成果 ⇒ 取消评奖资格"。
- 收口跑 `comp_precheck` 全项 PASS + 用户人工终审。

---

## 16. 交稿收口协议（截稿前如何不翻车）

来源：`report/REMAINING_TASKS.md`。要点：

- **硬交付物清单**：`result1–4.xlsx` + `支撑材料.zip` + 论文，缺一即不完整；逐项打勾、标依赖。
- **"冻结事实"表**：已确认可引用的关键数字单独成表、标"写论文直接引用勿再改动"，防临门重算。
- **"可立即做"分流**：不依赖未产出数字的项（合规/匿名/打包/图表规范）先做，别堵在最后一个数字上。
- **需用户提供的项（红线）**：AI 工具名/版本、用途表述档、交互示例、赛区附加要求——代理不可代填，编造即触红线。
- **收口命令按序**：占位符清零 → 交付物体检 → 重打包 → 数字对账/跨文件一致 → 文献核验 → 图表规范 → 降重 → 一键计分板。
- **风险登记按时限判断**：最大风险 = 数字没跑完（第 2–4 问无表 3–6 ⇒ 大幅失分）；按剩余时钟决定"继续算 vs 先保合规交付"。

---

## 17. 尺度与判据先行（不先写方程的"判据生成器"）

来源：`report/R2Y_SCALES.md`（`r2y_` 尺度标定队：**不求解 PDE，只产出与任何实现无关的解析判据**）。动手写数值代码之前，先派一路把**特征尺度表和"判据句"生成出来**，任何求解器解完都必须逐条对照这些判据，命中"若不满足"先怀疑实现、再怀疑物理。

- **先做量纲/尺度标定**（`手算自学手册` §3 同款）：热扩散率 `α=k/(ρc_p)`、扩散长度 `l_C=D/h_m`、毕渥数 `Bi=hR/k` 与 `Bi_m=h_mR/D`、渗透深度 `2√(Dt)`。它们一句话告诉你**该用什么模型**（Bi≪0.1 才可集总；本题 Bi≈1.4 ⇒ 必须解 PDE）、**1800 s 内水分只动最外 0.6 cm**。
- **篇幅样板**（写出下面这些，就是一套"判据生成器"）：
  - **瓶颈判据** `D/(h_m·R)`：<1 ⇒ 内扩散主导（不是表面膜）。
  - **时间尺度用过程积分，不是单点 `R²/D`**：单点在 C=2.55→0.15 跨 4 个量级，过程积分才能说清"2–3 天"。
  - **刚性来源**：`∂lnD/∂lnC=0.45/C`（低 C 段 3–9 倍）≫ `∂lnD/∂T=0.0425/K` ⇒ 刚性在 C 不在 T；1 K 温差 = 4.2% 的 D 误差。
  - **显式稳定限要同时算热侧与质侧**：热侧 `Δr²/(2α_max)` 常比质侧紧 ~16 倍；只看质侧会高报 2.4–88 倍裕度。
  - **网格要解析薄层**：`Δr ≤ min(l_T,l_C)/10`；低 C 段 l_C 塌缩到 1 mm，第 3 问必须加密到 0.1 mm。
- **判据句要"可证伪 + 给量化上下限"**：每条写"应落在 X–Y 区间；若超出 → 列 2–3 个最可能的实现缺陷"。例：`C_s(3h) 应在 0.3–0.7，若 <0.15 是把 g_w 当 g_d 用（差 ρ≈976 倍），若 >1.5 是边界写反`。
- **标定路自己的"自证局限"也必填**：穿透理论在 t>1.19 h 水量守恒失效 ⇒ 该判据只能当上限，不得当交付数字。

---

## 18. 数据清单与交付模板契约

来源：`report/data_inventory.md`。读题第一步的产品是一份可拍板的清单：

- **附件逐项登记表**：`文件 | sheet | 形状(行×列) | 起止/步长 | 物理含义 | 用途`。清单上没有的附件 = 没读，禁止进建模。
- **逐表"实测统计"而非印象**：完整性（有无缺/重复时间戳）、取值范围、单调性（如附件 1 末段非严格单调=实测噪声）、平台特征（附件 2 在 54 h 后稳定 ⇒ 题目预期烘干 54–72 h，可作独立合理性判据）。
- **交付模板行列契约（防"数值全对但错位一格"）**：工作表名、A1 表头、列头（0…2.0 步长 0.1）、A 列时间轴、末列口径（result4 末列是「药材表面」动边界，不是固定 2.0）——逐个单元格回填、保留四位小数。
- **任何论文数字都必须能回溯**：题面/附件原文 或 `out/results.json` 的具体键；找不到出处的一律不得出现。

---

## 19. 数字—来源对账协议

来源：`report/prov_reconciliation.md`（`prov_` 来源标签复核队）。不是"抽查"，是**逐条清点 + 独立复算 + 落到文件行号**：

- **五档判定**：`有据`（可追题面/附件原文或代码落盘文件+键）／`推导值·有据`（由有据数字算出且经独立复算）／`外部值未声明`（题面未给、取了外部标准值但未声明）／`估算未标注`／`无来源`（凭空出现、与自身公式不自洽，最严重）。
- **铁律：一个输入无来源 ⇒ 整条推导链判"无来源"**（分子 0.101 是符号写反实验的产物、分母 8.77 口径未声明 ⇒ "1.2%"整条作废）。
- **复核是只读 + 独立复算**：只新建自己前缀文件、不修改任何既有文件；用自写脚本复算每个数字，逐条落到"文件 + 行号 + 键"。
- **对账清单要在最前面写"版本声明"**：复核期间若被复核文件被他人改写（判据文件 16:23 是 119 行、16:29 变 178 行），必须按文件字节/时间戳声明"我复核的是哪个版本、新版本如何影响结论"。
- **比例子本身也要溯源**：`端面 7.4%`、`残差 3.2e-15`、`U_C=7.99e-7` 这些"看起来对"的数，逐条复算才发现其中若干无出处或错一位。

---

## 20. 判据的判据（门禁/判据本身也要被独立复核）

来源：`report/prov_reconciliation.md` §5.2 的发现。**门禁判据是人类/前一路写的，它也会错**，而且错了会误导大家去"修"本来正确的代码：

- **实例**：项目级门禁"一步后最外层 T[N−1]=28.8305 ℃"被多处复制为硬判据；独立复核证明**任何正确实现的一步结果都不是 28.8305**——正确完整隐式是 28.3796（差 0.451 ℃，容差 0.01 的 45 倍）。28.8305 来自"把最外层与内层解耦"的近似；它自己的 L37 还给出"环境=初值时一步后 28.4148"这种**自我否证**（凸组合直接要求仍为 28.000）。
- **任何判据都要过两条自检**：① 环境≡初值时，解必须还是初值（凸组合推论）；② 判据本身是否隐含了一个未声明的简化（解耦/恒定口径）。
- **"什么不是有效检验"要写进纪律**：守恒审计**不能替代**退化判据（它守恒的是"错的那个量"，表面公式错了照样通过）；用同一套表面公式写的几个代码互相验证，**不是独立验证**（会一致地错）。

---

## 21. 代码审计探针（最小变异 + 数值指纹 + 反事实/三极限/比较原理）

来源：`report/q2d2_code_audit.md`（D2 代码符号审计）。一把"只读代码 + 最小可判定数值探针"就能把符号/口径/死参数这类硬错误钉死：

- **最小变异探针**：把主循环**逐行忠实复制**成变体，只插入/切换**一行**可疑项（如 `bT -=` vs `+=`），用真实环境跑全程——实测反常值 36.8684 与写反变体 36.868444 **吻合到 5 位有效数字** ⇒ 成因即符号写反。**数值指纹 + 最小变异 = 最硬的归因。**
- **反事实对照**：把可疑项同时反号（对角与右端一起 `-=`），看效果是否反转——反了即证原式符号正确。
- **三极限实测**：A `T_air` 高、场低 → 表面应升温；A′ 反号 → 应降温（反证）；B `h→∞` → 膜口径真表面应贴环境；C 环境=初值 → 场不动（但**对符号不敏感**，只能排漂移）。**每个极限写明它的判别力边界。**
- **比较原理否证"自洽"辩解**：在 M-矩阵（对角占优、非对角非正）下，加一个**汇**源项的解必须逐点、逐时**不超过**无源解——"净效应升温却自称自洽"直接被否。
- **先查"这一段到底存不存在"**：被点名的文件里 `L_v` 出现 0 次、`latent=True` 与 `False` 结果**逐位相同** ⇒ 所谓"计入潜热"是**死参数**，交付链路与声明不符。审计的第一步永远是全文 grep，不是"理解意图"。
- **六文件交叉对照表**：同一符号模式在六个实现里逐一列出「通量定义→应用行→净效果→判定」，谁乘了 ρ、谁符号反了、谁用温度松弛浓度（放大 24 倍）一目了然。

---

## 22. 数字对账的落地输出（严重度分级 + 谁改谁复核 + 勿改清单）

来源：`report/prov_corrections.md`（来源复核队的执行清单）。"五档判定"（§19）之后，**落地输出要长这样**：

- **严重度 P0–P3**：`P0` 凭空无来源数据 → **必须改数字**；`P1` 外部值未声明 → 补声明+补灵敏度；`P2` 估算未标注 → 补标签（被下游引用则升 P0）；`P3` 口径/引用错位（数字有据）→ 只改表述。每条写：**文件 + 行 + 现状逐字原文 + 问题 + 建议改法 + 由谁改 + 由谁复核**。
- **"由谁改"与"由谁复核"必须分离**（复核队不得是执行队，也不得是同一方）；**先落盘正确值、再按落盘值改写文字**，不许"边改边猜"。
- **列出"勿改清单"（防误改）**：对账报告末尾必须单列"确认**无需修改**的高质量处"——如某段通篇无 `L_v`、某表逐数字带标签——**防止后续队伍把对的也"顺手改"掉**。
- **"数字修辞"是错误的重灾区**：`放大 100 倍`（实为 388）、`放大 20 倍`（实为 5.4）、`24 倍`（无法复现）、`2326 W/m²`（全库 grep 零命中）——**"看起来对"的整数倍数常是无来源数的载体**；凡倍数/百分比/取整量必须给出算式并复算。
- **执行顺序做成依赖 DAG**（哪个改动作依赖哪个前置，可并行项标出），领队据此派单，别让"改门禁"和"改潜热"互相卡。

---

## 附录 A：量化失败台账（症状 → 根因 → 便宜抓法 → 后果）

| # | 症状 | 根因 | 便宜抓法 | 后果量级 |
|---|---|---|---|---|
| 1 | Q3 烘干 16.4h（应为 57h） | PDF 分数指数读反 `e^-0.45C` vs `e^-0.45/C` | 视觉读图 + grep + 物理量级反推 | 差 3.5 倍 |
| 2 | 温度/水分场一步崩到 0/NaN | 线性化不自洽 / 表面半单元显式超限 | UT2 均匀场 / 半单元 Δt 判据 | 全解作废 |
| 3 | 潜热算成"2.3% 小项"（应 18.9 倍） | 漏乘 ρ_dry（混干基/水质口径） | G-1 显式报 g_w=ρ·g_d | 结论反号 |
| 4 | 温度"合理"但水分场锁死 | 场没吃到边界通量 | G-4 边界—场自洽，差 5e8 倍 | 一票否决 |
| 5 | Q4 100h 未达判据（被当物理结论） | 同套交付物 Q3/Q4 口径不一致 | 同系统变体量级自洽核对 | 错误论文结论 |
| 6 | 四路互差"≤0.032℃"复现不出 | 口径修复前后中间产物差被张冠李戴 | 数字必须可复现 + 注明出处 | 证据链断裂 |
| 7 | 16.4h + 三重验证 + 灵敏度"自洽" | 公式读反，但只做了求解器级验证 | 第①层解读正确性 + 独立实现交叉 | 高可信错误报告 |
| 8 | 同一个数 6 个版本互相矛盾 | 估算值被当事实传播、无溯源 | 数字可靠度分级 + ledger | 全篇失信 |

> 完整失败案例与溯源证据见本仓库 `docs/failure-ledger.md`；工作区盘点见 `docs/workspace-inventory.md`；复盘叙事见 `docs/lessons-learned.md`。